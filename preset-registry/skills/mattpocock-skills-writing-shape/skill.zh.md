---
name: writing-shape
description: Writing, exploit — shape raw material into an article, paragraph by paragraph.
disable-model-invocation: true
---
收到，我先按流程确认：  
请先告诉我当前任务要使用哪些 **skill / plugin 组**（或具体 skill），例如：

- 只用默认空载（不启用任何组）
- 启用某一整组（如 `baoyu-skills` / `lark` / `openspec`）
- 只启用具体 skill（你直接给我名字）

确认后我再直接输出该段的中文翻译。
