---
name: review
description: Review the changes since a fixed point (commit, branch, tag, or merge-base) along two axes — Standards (does the code follow this repo's documented coding standards?) and Spec (does the code match what the originating issue/PRD asked for?). Runs both reviews in parallel sub-agents and reports them side by side. Use when the user wants to review a branch, a PR, work-in-progress changes, or asks to "review since X".
---
已收到你需要使用受管插件的场景。  
请先确认：你想在当前任务中启用哪些 `skill` / `plugin`？（未加载前我先不执行正文处理）

- 任务当前命中：`setup-matt-pocock-skills`（来自插件组 `matt-pocock-skills`）
- 建议按 `loadout` 选项确认其一：
  - 仅启用 `matt-pocock-skills` 整组（含全部 skill）
  - 仅启用 `setup-matt-pocock-skills`
  - 不启用该组/skill（若你同意按原样只做离线翻译）

请用你的 `loadout-manager` 完成确认并回我你选择。
