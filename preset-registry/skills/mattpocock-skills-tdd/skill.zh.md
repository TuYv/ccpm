---
name: tdd
description: Test-driven development. Use when the user wants to build features or fix bugs test-first, mentions "red-green-refactor", or wants integration tests.
---
已收到。你的输入命中了受管 skill（`codebase-design`、`code-review`，所属组 `matt-pocock-skills`，当前未加载），我先不执行翻译。

请先确认是否执行 `$loadout-manager` 并选择：

- 仅加载 `codebase-design`
- 仅加载 `code-review`
- 同时加载 `codebase-design` 与 `code-review`
- 不加载受管 skill，继续直接翻译这段文本
